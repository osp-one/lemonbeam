// Future full-guide generator.
// This will run each guide section, combine the results, and collect uncertainties.
//
// TODO (BYOK):
// - accept the user-supplied OpenAI API key (or an already-built client) as
//   a parameter and thread it into every generateGuideSection.ts call
// - do not read a shared server-side key here
export {}
